import React from 'react'
import { css } from "../../styled-system/css"

const Header = () => {
  return (
    <header className={
      css({
        color: "red"
      })
    }>
      Header
    </header>
  )
}

export default Header